# Privacy Shield AI Backend

Local Express server that proxies requests to Ollama for privacy policy analysis.

## Run

```bash
npm install
npm run dev
```

## Environment

- `PORT` (default: 8787)
- `OLLAMA_BASE_URL` (default: http://localhost:11434)
